function textParagraph(text) {
    let p = `<p>${text}</p>`;
    document.write(p);
}
textParagraph('super famous text for reading');
textParagraph('and read it again');
textParagraph('just one more time');